void main() {   
  print('Type a number:');
  int number = int.parse(stdin.readLineSync()!);
  //List<int> resultNum = [1, 2, 3, 4, 5, 6];
  for(var i = 1; i <= 10; i++ ){
    var divisionNum = number * i;
    if(divisionNum % number == 0){
      print(divisionNum);
    }
  }  
}
